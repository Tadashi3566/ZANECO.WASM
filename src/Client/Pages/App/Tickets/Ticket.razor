@page "/app/ticket/{id:guid}"

@attribute [MustHavePermission(FSHAction.View, FSHResource.Tickets)]

<FshTitle Title="Ticket" Description="Ticket management and progress" />

<TicketComponent Id="@Id" />